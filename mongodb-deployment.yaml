apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb
  namespace: fitness-app
spec:
  replicas: 4
  selector:
    matchLabels:
      app: mongodb
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: mongodb
    spec:
      containers:
      - name: mongodb
        image: mongo:7-jammy
        ports:
        - containerPort: 27017
        volumeMounts:
          - name: fit-volume
            mountPath: /data/db
        resources:
          requests:
            memory: "128i"
            cpu: "100m"
          limits:
            memory: "130Mi"
            cpu: "200m"
      volumes:
        - name: fit-volume
          emptyDir: 
            sizeLimit: 512Mi
          
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: fitness-app
spec:
  selector:
    app: mongodb
  ports:
  - port: 27017
    targetPort: 27017
  type: ClusterIP
 
 